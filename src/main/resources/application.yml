spring:
  datasource:
    username: postgres
    password: qwfb@iuwfJQND
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    hikari:
      schema: task_manager_app_schema

  jpa:
    properties:
      hibernate:
        default_schema: ${spring.datasource.hikari.schema}
